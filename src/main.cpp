#include <iostream>
#include "project1.h"


/**
 * \brief Simple wrapper for libproject1 containing the main function
 * 
 * @param[in] argc 
 * @param[in] argv 
 */
int main (int argc, char *argv[])
{ 
    int status = 0;
    
    std::cout << "Usage: project1 ...\n";
    return status;
}